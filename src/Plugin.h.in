#pragma once

#include <string_view>

namespace Plugin
{
	using namespace std::literals;

	inline constexpr REL::Version VERSION{ @PROJECT_VERSION_MAJOR@u, @PROJECT_VERSION_MINOR@u, @PROJECT_VERSION_PATCH@u };

	inline constexpr std::size_t VERSION_RELEASE = @PLUGIN_VERSION@;

	inline constexpr auto NAME = "@PROJECT_NAME@"sv;
}
